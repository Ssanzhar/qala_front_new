import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#F0FDFA",
    },
    secondary: {
      main: "#dc004e",
    },
  },
  typography: {
    fontFamily: "Montserrat",
  },
});
